# =============================================================================
# High-Availability Failover Configuration
# =============================================================================
# Copy this file to config.env and fill in your values:
#   cp config.env.example config.env
#
# This file is gitignored - your actual config.env will not be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Primary server - where the main application runs
PRIMARY_HOST=your-primary-server-ip
PRIMARY_USER=your-ssh-username

# Backup server - standby for failover (REQUIRED for high availability)
BACKUP_HOST=your-backup-server-ip
BACKUP_USER=your-ssh-username

# Load balancer server (optional - for full 3-tier setup)
LB_HOST=
LB_USER=

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
APP_NAME=coffee
APP_PORT=3000
HEALTH_ENDPOINT=/coffees

# Docker container names (derived from APP_NAME by default)
APP_CONTAINER=${APP_NAME}_app
DB_CONTAINER=${APP_NAME}_db

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DB_PORT=5432
DB_NAME=coffee_dev
DB_USER=postgres
DB_PASSWORD=postgres

# -----------------------------------------------------------------------------
# Project Paths
# -----------------------------------------------------------------------------
# Directory name where the project is cloned on servers
PROJECT_DIR=high-availability-failover

# Subdirectory containing the application code
APP_DIR=coffee_project

# -----------------------------------------------------------------------------
# Monitoring & Failover Configuration
# -----------------------------------------------------------------------------
# Health check interval in seconds
HEALTH_CHECK_INTERVAL=30

# Number of failed health checks before triggering failover
FAIL_THRESHOLD=3

# Database replication interval in minutes
REPLICATION_INTERVAL=30

# -----------------------------------------------------------------------------
# Domain Configuration (Optional)
# -----------------------------------------------------------------------------
# Leave empty to use IP-based access
DOMAIN_NAME=

# Set to true to enable HTTPS (requires DOMAIN_NAME)
USE_HTTPS=false

# Email for Let's Encrypt certificate (required if USE_HTTPS=true)
SSL_EMAIL=

# -----------------------------------------------------------------------------
# Cloudflare Tunnel (Optional - alternative to direct domain)
# -----------------------------------------------------------------------------
TUNNEL_NAME=
TUNNEL_CREDENTIALS_FILE=
