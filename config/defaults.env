# =============================================================================
# Default Configuration Values
# =============================================================================
# These are the default values used by scripts when config.env is not present
# or when specific values are not set in config.env.
#
# DO NOT put sensitive information here - this file is committed to git.
# =============================================================================

# Application Configuration
APP_NAME="${APP_NAME:-coffee}"
APP_PORT="${APP_PORT:-3000}"
HEALTH_ENDPOINT="${HEALTH_ENDPOINT:-/coffees}"

# Container names (derived from APP_NAME)
APP_CONTAINER="${APP_CONTAINER:-${APP_NAME}_app}"
DB_CONTAINER="${DB_CONTAINER:-${APP_NAME}_db}"

# Database Configuration
DB_PORT="${DB_PORT:-5432}"
DB_NAME="${DB_NAME:-coffee_dev}"
DB_USER="${DB_USER:-postgres}"

# Project Paths
PROJECT_DIR="${PROJECT_DIR:-high-availability-failover}"
APP_DIR="${APP_DIR:-coffee_project}"

# Monitoring Configuration
HEALTH_CHECK_INTERVAL="${HEALTH_CHECK_INTERVAL:-30}"
FAIL_THRESHOLD="${FAIL_THRESHOLD:-3}"
REPLICATION_INTERVAL="${REPLICATION_INTERVAL:-30}"

# Domain Configuration
DOMAIN_NAME="${DOMAIN_NAME:-}"
USE_HTTPS="${USE_HTTPS:-false}"

# Derived paths (set after loading config)
if [ -n "${PRIMARY_USER:-}" ]; then
    PROJECT_PATH="/home/${PRIMARY_USER}/${PROJECT_DIR}"
    APP_PATH="${PROJECT_PATH}/${APP_DIR}"
fi

# Log paths
LOG_DIR="${LOG_DIR:-/var/log/${APP_NAME}-failover}"
BACKUP_DIR="${BACKUP_DIR:-/tmp/db-backup}"
REPLICATION_DIR="${REPLICATION_DIR:-/tmp/${APP_NAME}-replication}"
